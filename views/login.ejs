<!doctype html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Log-In(เข้าสู่ระบบ)</title>

    <!-- add icon link -->
    <link rel="icon" href="images/LogoMakr-18GTI0.png" type="image/x-icon">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
</head>

<body>
    <div class="container bg-light">
        <div>
            <h1 style="background-color: rgb(83, 142, 219);">เข้าสู่ระบบ</h1>
        </div>
        <br>
        <% if (!locals.logedIn) { %>
            <h4>กรุณากรอกอีเมล์และรหัสผ่าน</h4><br>
            <form method="post" action="/login">
                <div class="form-floating mb-3 col-md-6">
                    <input type="email" class="form-control" name="login" id="floatingInput"
                        placeholder="name@example.com" value="<%= login %>" required>
                    <label for="floatingInput">อีเมล์</label>
                </div>
                <div class="form-floating col-md-6">
                    <input type="password" class="form-control" name="password" id="floatingPassword"
                        placeholder="Password" value="<%= password %>" required>
                    <label for="floatingPassword">รหัสผ่าน</label>
                    <input type="checkbox" onclick="pwView()">แสดงรหัสผ่าน
                </div>
                <br><br>
                <div>
                    <input type="checkbox" name="save" value="checked" <%= save %>>เก็บไว้ในเครื่องนี้
                </div>
                <br><br>
                <div>
                    <button type="submit" id="btLogIn" class="btn btn-primary">เข้าสู่ระบบ</button>
                </div>
            </form>
            <% } %>

                <div id="result">
                    <% if (login && logedIn) { %>
                        สวัสดีค่ะ คุณ <%= login %> <br>
                            <h4>คุณเข้าสู่ระบบแล้ว</h4>

                            <% } else if (locals.valid==false) { %>
                                <h4 style="color:red">คุณใส่ Email หรือ Password ไม่ถูกต้อง</h4>
                                <% } %>
                </div>
                <br><br>
                <div class="row">

                    <form action="" method="post" id="formAction">
                        <button type="submit" onclick="home()" class="btn btn-primary">กลับหน้าหลัก</button>

                        <button type="submit" onclick="signin()" class="btn btn-primary">สมัครสมาชิกใหม่</button>

                    </form>
                </div>
                <br><br>
    </div>

    <script>
        function pwView() {
            let x = document.getElementById("floatingPassword");
            if (x.type === "password") {
                x.type = "text";
            } else {
                x.type = "password";
            }
        }

        function home () {
            var home = document.getElementById('formAction')
            home.action = '/'
        }

        function signin () {
            var signin = document.getElementById('formAction')
            signin.action = '/signin'
        }
    </script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa"
        crossorigin="anonymous"></script>
</body>

</html>